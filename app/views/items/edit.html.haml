- i = 1
- @item.images.each do |image|
  %br 
  = i
  = image.filename
  - i += 1
.single-box
  %header.single-box__header
    %h1.single-box__header__logo
      = link_to root_path do
        = image_tag "logo.svg", alt: "mercari"
  %main.single-main__box__sell
    %section.single-main__box__sell__inner
      %h2.single-main__box__sell__inner__head
        商品の情報を編集
      = form_with model: @item, remote: true, class: "single-main__box__sell__inner__form", id: "form_with_edit"  do |f|
        .single-main__box__sell__inner__form__upload
          %h3.single-main__box__sell__inner__form__upload__head
            出品画像
            %span.form-require
              必須
          %p 
            最大10枚までアップロードできます
            %span
          .single-main__box__sell__inner__form__upload__items
            .single-main__box__sell__inner__form__upload__items__box
              .single-main__box__sell__inner__form__upload__items__box__have#preview_box
                %ul#preview_list
                  - i = 0
                  - @item.images[0..4].each do |image|
                    %li.upload-sell-item{id: i}
                      - i += 1
                      %figure.upload-sell-figure
                        = image_tag(image)
                      .upload-sell-btn
                        %a.upload-sell-edit="編集"
                        %a.upload-sell-delete="削除"
                %ul#preview_list2
                  - i = 5
                  -if @item.images.length > 5
                    - @item.images[5..9].each do |image|
                      %li.upload-sell-item{id: i}
                        - i += 1
                        %figure.upload-sell-figure
                          = image_tag(image)
                        .upload-sell-btn
                          %a.upload-sell-edit="編集"
                          %a.upload-sell-delete="削除"
              -if @item.images.length == 0 || @item.images.length == 5
                = f.label :images do
                  .single-main__box__sell__inner__form__upload__drop-0#drop_box
                    = f.file_field :images, multiple: true, class: 'upload-sell-drop-file', accept: "image/jpeg, image/png"
                    .drop-field
                    %pre.drop-text 
                      ドラッグアンドドロップ
                      またはクリックしてファイルをアップロード
              -elsif @item.images.length == 1 || @item.images.length == 6
                = f.label :images do
                  .single-main__box__sell__inner__form__upload__drop-1#drop_box
                    = f.file_field :images, multiple: true, class: 'upload-sell-drop-file', accept: "image/jpeg, image/png"
                    .drop-field
                    %pre.drop-text 
                      ドラッグアンドドロップ
                      またはクリックしてファイルをアップロード
              -elsif @item.images.length == 2 || @item.images.length == 7
                = f.label :images do
                  .single-main__box__sell__inner__form__upload__drop-2#drop_box
                    = f.file_field :images, multiple: true, class: 'upload-sell-drop-file', accept: "image/jpeg, image/png"
                    .drop-field
                    %pre.drop-text 
                      ドラッグアンドドロップ
                      またはクリックしてファイルをアップロード
              -elsif @item.images.length == 3 || @item.images.length == 8
                = f.label :images do
                  .single-main__box__sell__inner__form__upload__drop-3#drop_box
                    = f.file_field :images, multiple: true, class: 'upload-sell-drop-file', accept: "image/jpeg, image/png"
                    .drop-field
                    %pre.drop-text 
                      ドラッグアンドドロップ
                      またはクリックしてファイルをアップロード
              -elsif @item.images.length == 4 || @item.images.length == 9
                = f.label :images do
                  .single-main__box__sell__inner__form__upload__drop-4#drop_box
                    = f.file_field :images, multiple: true, class: 'upload-sell-drop-file', accept: "image/jpeg, image/png"
                    .drop-field
                    %pre.drop-text 
                      ドラッグアンドドロップ
                      またはクリックしてファイルをアップロード
              -else
                = f.label :images do
                  .single-main__box__sell__inner__form__upload__drop#drop_box
                    = f.file_field :images, multiple: true, class: 'upload-sell-drop-file', accept: "image/jpeg, image/png"
                    .drop-field
                    %pre.drop-text 
                      ドラッグアンドドロップ
                      またはクリックしてファイルをアップロード
        .single-main__box__sell__inner__form__index-box
          .form-group
            %label
              商品名
              %span.form-require
                必須
            %div
              = f.text_field :name, placeholder: "商品名(必須 40文字まで)", class: "input-default"
          .form-group
            %label
              商品の説明
              %span.form-require
                必須
            %div
              = f.text_area :introduct, class: "textarea-default", rows: 5, placeholder: "商品の説明（必須 1,000文字以内）（色、素材、重さ、定価、注意点など）例）2010年頃に1万円で購入したジャケットです。ライトグレーで傷はありません。あわせやすいのでおすすめです。"
        .single-main__box__sell__inner__form__index-box
          %h3.item-sub-head
            商品の詳細
          .item-form-box
            .form-group#category-select-box_list
              %label
                カテゴリー
                %span.form-require
                  必須
              .select-wrap#parent_box
                %i.icon-arrow-bottom
                = f.collection_select :parent_id, @parents, :id, :name,{prompt: "---"}, class: "select-default", id: "parent_form"
              .form-group#size_box
                %label
                  サイズ
                  %span.form-arbitrary
                    任意
                .select-wrap
                  %i.icon-arrow-bottom
                  = f.select :size, options_for_select(Item.sizes.keys), {}, class: "select-default"
              .form-group#brand_box
                %label
                  ブランド名
                  %span.form-arbitrary
                    任意
                %div
                  = f.text_field :hoge_id, placeholder: "例）シャネル", class: "input-default", id: "input_brand_box", name: "brand_name"
                  = f.hidden_field :brand_id
                  #search_brand_box
                    %ul#search_brand_result
            .form-group
              %label
                商品の状態
                %span.form-require
                  必須
              .select-wrap
                %i.icon-arrow-bottom
                = f.select :condition, options_for_select(Item.conditions.keys), {}, class: "select-default"
        .single-main__box__sell__inner__form__index-box
          %h3.item-sub-head
            配送について
          = link_to root_path, target: "_blank", class: "form-question" do
            ?
          .item-form-box
            .form-group#postage-select-box_list
              %label
                配送料の負担
                %span.form-require
                  必須
              .select-wrap#postage_box
                %i.icon-arrow-bottom
                = f.collection_select :parent_id, @postages, :id, :name, {prompt: "---"}, class: "select-default", id: "postage_form"
            .form-group
              %label
                発送元の地域
                %span.form-require
                  必須
              .select-wrap
                %i.icon-arrow-bottom
                = f.collection_select :prefecture_id, Prefecture.all, :id, :name, {}, class: "select-default"
            .form-group
              %label
                発送までの日数
                %span.form-require
                  必須
              .select-wrap
                %i.icon-arrow-bottom
                = f.select :shipping_date, options_for_select(Item.shipping_dates.keys), {}, class: "select-default", default: (@item.shipping_date)
        .single-main__box__sell__inner__form__index-box
          %h3.item-sub-head
            販売価格(300〜9,999,999)
          = link_to root_path, target: "_blank", class: "form-question" do
            ?
          .item-form-box
            %ul.sell-price
              %li.form-group
                .clearfix
                  %label.left-text
                    価格
                    %span.form-require
                      必須
                  .yen-mark
                    ¥
                    .input-price
                      = f.text_field :price, class: "input-default", placeholder: "例）300"
              %li.clearfix#sale-tax
                .left-text
                  販売手数料 (10%)
                .right-text#tax-text
                  ー
              %li.clearfix.bold
                .left-text
                  販売利益
                .right-text#profit-text
                  ー
        .single-main__box__sell__inner__form__index-box
          .attention-text
            %p
              禁止されている出品、行為を必ずご確認ください。
            %p
              またブランド品でシリアルナンバー等がある場合はご記載ください。偽ブランドの販売は犯罪であり処罰される可能性があります。
            %p
              また、出品をもちまして加盟店規約に同意したことになります。
          = f.submit "出品する", class: "btn-default btn-red", id: "item-submit"
          = link_to root_path, class: "btn-default btn-gray" do
            戻る
  = render "shared/logo_footer"
